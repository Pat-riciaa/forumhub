spring.application.name=forumhub
# Porta padr�o
server.port=8080

# Configura��o do banco H2 em mem�ria
spring.datasource.url=jdbc:h2:mem:forumhubdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Mostrar SQL no console (opcional)
spring.jpa.show-sql=true

# Criar ou recriar as tabelas automaticamente a cada inicializa��o
spring.jpa.hibernate.ddl-auto=create

# Para que o H2 console fique dispon�vel no navegador
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Configura��o para rodar scripts SQL s� depois que as tabelas existirem

